@*@model Vidly.ViewModels.CustomerViewModel*@
@{
    ViewBag.Title = "Customers";
}

<h2>@ViewBag.Title</h2>

<p>
    <a href="/Customers/NewCustomer">
        <button class="btn btn-primary btn-lg">New</button>
    </a>
</p>

@*@if (Model.Customers.Count() == 0)
{
    <h2>No Customers</h2>
}
else
{*@
    <table class="table table-striped table-hover table-bordered" id="customers">
        <thead>
            <tr>
                <th>Customer Name</th>
                <th>Membership Type</th>              

            </tr>
        </thead>
        <tbody>
            @*@foreach (var i in Model.Customers)
            {
                <tr>
                    <td>
                        <a href="/Customers/EditCustomer/@i.Id">@i.Name</a>

                    </td>

                    <td>@i.MembershipType.Name</td>

                    <td>
                        <button data-customer-id="@i.Id" class="btn-link js-delete">Delete</button>
                    </td>

                </tr>
            }*@
        </tbody>
    </table>
@*}*@

@section scripts
{
    <script>
        $(document).ready(() => {
            var table = $("#customers").DataTable({
                ajax: {
                    url: "/api/customers",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "name",
                        render: function (data, type, customer) {
                            return "<a href='customers/ReadOnly/" + customer.id + "'>" + customer.name + "</a>";
                        }
                    },
                    {
                        data: "membershipType.name",
                    }
                ]
            });
        
         });
    </script>
    
}

